/*
 * bluetooth.c
 *
 *  Created on: Feb 24, 2013
 *      Author: trol
 */

//#include "bluetooth.h"

#include <avr/io.h>
#include <util/delay.h>

//#include "uart.h"






/*
	// Code to be executed when the USART receives a byte here
	ISR(USART_RXC_vect)	{
		uint8_t receivedByte;
		receivedByte = UDR;
		buf[bufPos++] = receivedByte;
		UDR = receivedByte;
		if (bufPos >= 2 && buf[bufPos-2] == 'h' && buf[bufPos-1] == 's') {
			for (uint8_t i = 0; i < bufPos; i++) {
				uart_putc(buf[i]);
			}
		}
	}
*/





